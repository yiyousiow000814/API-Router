--- a/webview/assets/index-D-3V455n.js
+++ b/webview/assets/index-D-3V455n.js
@@ -151626,6 +151626,18 @@
         }),
       [e],
     ),
+    (0, Z.useEffect)(() => {
+      let t = () => {
+        e.set(Tu, !1);
+      };
+      return (
+        (window.__ELECTRON_SHIM__.closeSidebar = t),
+        () => {
+          window.__ELECTRON_SHIM__.closeSidebar === t &&
+            delete window.__ELECTRON_SHIM__.closeSidebar;
+        }
+      );
+    }, [e]),
     (0, Z.useEffect)(() => {
       let a = n.map((n) => {
         let a = e.get(_ee, n.key);
