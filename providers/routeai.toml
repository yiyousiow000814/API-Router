id = "routeai"

[match]
base_url_host_suffixes = ["routeai.cc"]

[usage]
refresh_flow = "auto"
budget_info_auth_source = "usage_token"
candidate_base_sources = ["explicit_usage_base_url"]
explicit_endpoint_mode = "none"

[package_expiry]
strategy = "none"
